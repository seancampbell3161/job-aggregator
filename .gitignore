.worktrees/
.superpowers/
.venv/
__pycache__/
*.pyc
.pytest_cache/
build/
dist/
*.egg-info/
lambda.zip
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
# Personal deployment config + relevance profile (personal — never commit).
# Copy config.example.yaml / profile.example.md as templates.
config.yaml
profile.md
# Personal résumé for gap analysis (PII — never commit). Use resume.md.example as a template.
resume.md
resume/content.json
resume/evidence.json
resume/facts.yaml
resume/baseline.html
tailored/
data/
fingerprint-results-*.json
# Docker secrets — never commit
.env
resume/templates/
# macOS / ad-hoc backups
.DS_Store
*.bak-*
# Local-only design specs & implementation plans (not published)
docs/superpowers/
# Tool-generated mirror of .claude/skills/ — never commit
.agents/
